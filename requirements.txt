# ──────────────────────────────────────────────────────────────────────────────
# requirements.txt — Dependencias del sistema de analítica de tráfico vehicular
# ──────────────────────────────────────────────────────────────────────────────

# Visión artificial y captura de video
opencv-python>=4.9.0

# Detección de objetos con YOLO (incluye soporte OpenVINO vía --extra-index-url)
ultralytics>=8.2.0

# Tracking (ByteTrack) y lógica de zonas (LineZone)
supervision>=0.21.0

# Backend API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.32

# Utilidades
numpy>=1.26.0
Pillow>=10.3.0   # Requerido por supervision para algunas operaciones de imagen

# ── OpenVINO (opcional, descomentar si se usa NPU/iGPU Intel) ─────────────────
# openvino>=2024.1.0
# openvino-dev>=2024.1.0
